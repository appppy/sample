#include <gtest/gtest.h>

#include "example.h"

TEST(example_test, memcpy1) {
    ASSERT_EQ(0, memcpy1());
}

TEST(example_test, memcpy2) {
    ASSERT_EQ(0, memcpy2());
}

TEST(example_test, memcpy3) {
    ASSERT_EQ(0, memcpy3());
}

TEST(example_test, memcpy4) {
    ASSERT_EQ(0, memcpy4());
}

TEST(example_test, memcpy5) {
    ASSERT_EQ(0, memcpy5());
}

TEST(example_test, memcpy6) {
//    ASSERT_EQ(0, memcpy6());
}

TEST(example_test, memcpy7) {
    ASSERT_EQ(0, memcpy7());
}

TEST(example_test, memcpy8) {
    ASSERT_EQ(0, memcpy8());
}
